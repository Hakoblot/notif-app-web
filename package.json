{
  "name": "notif-app-web",
  "version": "1.0.0",
  "private": true,
  "description": "Simple web dashboard and API for Expo push notification testing.",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "check": "node --check server/index.js && node --check server/db.js && node --check server/push.js && node --check public/app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "expo-server-sdk": "^6.1.0",
    "express": "^5.2.1",
    "mysql2": "^3.22.2"
  }
}
